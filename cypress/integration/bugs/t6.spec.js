/// <reference types="Cypress" />

context('file 6', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f6 - t1', () => {
    expect(true).to.be.true
  })

  it('f6 - t2', () => {
    expect(true).to.be.true
  })

  it('f6 - t3', () => {
    expect(true).to.be.true
  })

  it('f6 - t4', () => {
    expect(true).to.be.true
  })
})
