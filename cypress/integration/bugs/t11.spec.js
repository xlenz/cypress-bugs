/// <reference types="Cypress" />

context('file 11', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f11 - t1', () => {
    expect(true).to.be.true
  })

  it('f11 - t2', () => {
    expect(true).to.be.true
  })

  it('f11 - t3', () => {
    expect(true).to.be.true
  })

  it('f11 - t4', () => {
    expect(true).to.be.true
  })
})
