/// <reference types="Cypress" />

context('file 7', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f7 - t1', () => {
    expect(true).to.be.true
  })

  it('f7 - t2', () => {
    expect(true).to.be.true
  })

  it('f7 - t3', () => {
    expect(true).to.be.true
  })

  it('f7 - t4', () => {
    expect(true).to.be.true
  })
})
