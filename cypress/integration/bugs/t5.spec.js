/// <reference types="Cypress" />

context('file 5', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f5 - t1', () => {
    expect(true).to.be.true
  })

  it('f5 - t2', () => {
    expect(true).to.be.true
  })

  it('f5 - t3', () => {
    expect(true).to.be.true
  })

  it('f5 - t4', () => {
    expect(true).to.be.true
  })
})
