/// <reference types="Cypress" />

context('file 9', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f9 - t1', () => {
    expect(true).to.be.true
  })

  it('f9 - t2', () => {
    expect(true).to.be.true
  })

  it('f9 - t3', () => {
    expect(true).to.be.true
  })

  it('f9 - t4', () => {
    expect(true).to.be.true
  })
})
