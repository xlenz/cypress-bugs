/// <reference types="Cypress" />

context('file 8', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f8 - t1', () => {
    expect(true).to.be.true
  })

  it('f8 - t2', () => {
    expect(true).to.be.true
  })

  it('f8 - t3', () => {
    expect(true).to.be.true
  })

  it('f8 - t4', () => {
    expect(true).to.be.true
  })
})
