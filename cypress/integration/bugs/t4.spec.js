/// <reference types="Cypress" />

context('file 4', () => {
  beforeEach(() => {
    console.log('qwe')
  })

  it('f4 - t1', () => {
    expect(true).to.be.true
  })

  it('f4 - t2', () => {
    expect(true).to.be.true
  })

  it('f4 - t3', () => {
    expect(true).to.be.true
  })

  it('f4 - t4', () => {
    expect(true).to.be.true
  })
})
